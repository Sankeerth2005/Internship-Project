# Brand images

- `om_symbol.png` — Om mark with transparent background (use via `BrandIcons.om` / `BrandIconBadge.om`)
- `google_g.png` — Google G for sign-in buttons

Optional (fallback to Om if missing):
- `splash_screen.png` / `splash_logo.png`

Do not draw Om with text glyphs or grid overlays; always prefer these assets.
